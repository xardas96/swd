<Page x:Class="SWD_projekt.UI.GeoLocalizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="{StaticResource geoLocalizationTitle}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Label Content="{StaticResource geoLocalization}" Height="28" HorizontalAlignment="Left" Name="label1" VerticalAlignment="Top" FontWeight="Bold" />
        <TextBox Height="23" Margin="12,6,12,0" Name="addressBox" VerticalAlignment="Top" Grid.Row="1" />
        <RadioButton Content="{StaticResource shortDistance}" Grid.Row="1" Height="16" HorizontalAlignment="Left" Margin="12,35,0,0" Name="shortDistanceBox" VerticalAlignment="Top" IsChecked="True" />
        <RadioButton Content="{StaticResource largeDistance}" Grid.Row="1" Height="16" Margin="12,57,0,0" Name="largeDistanceBox" VerticalAlignment="Top" HorizontalAlignment="Left" />
    </Grid>
</Page>
